using UnityEngine;

public class boiledWaterAttack : MonoBehaviour
{
    //on collision explode
    //do damage


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Awake()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
}
